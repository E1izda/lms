# Frontend (React + Vite)

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Default dev server: http://localhost:5173

This minimal scaffold provides pages: Courses, Course detail, Login. It uses JWT tokens stored in `localStorage`.
